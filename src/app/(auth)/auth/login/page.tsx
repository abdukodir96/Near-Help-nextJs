import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function LoginPage() {
  return <PagePlaceholder eyebrow="Auth" title="Login route is ready." description="Next step is connecting login mutation, token storage and redirect guards." />;
}
