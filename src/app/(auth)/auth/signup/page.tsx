import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function SignupPage() {
  return <PagePlaceholder eyebrow="Auth" title="Signup route is ready." description="Next step is connecting signup mutation and role-aware onboarding flow." />;
}
