import { PagePlaceholder } from '@/components/shared/page-placeholder';

export default function NotificationsPage() {
  return <PagePlaceholder eyebrow="Notifications" title="Notifications page" description="This screen will connect to getNotifications, unread count and mark-as-read actions." />;
}
