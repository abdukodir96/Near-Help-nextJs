import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function NotificationsPage() {
  return <PagePlaceholder eyebrow="Notifications" title="Notifications route is ready." description="We will connect this route to getNotifications, unread count and mark-as-read flows." />;
}
