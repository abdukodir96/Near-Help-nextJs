import { PagePlaceholder } from '@/components/shared/page-placeholder';

export default function CommunityDetailPage() {
  return <PagePlaceholder eyebrow="Community / Detail" title="Article detail page" description="This screen will connect to getArticle, nested comments, replies and likes." />;
}
