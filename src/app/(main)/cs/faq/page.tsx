import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function FaqPage() {
  return (
    <PagePlaceholder
      eyebrow="CS FAQ"
      title="FAQ route is ready."
      description="We will connect this route to getNotices with noticeCategory=FAQ and render the support FAQ list."
    />
  );
}
