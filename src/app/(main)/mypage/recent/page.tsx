import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function MyRecentPage() {
  return <PagePlaceholder eyebrow="My Page" title="Recently visited route is ready." description="We will map this route to getVisited and render recently viewed services." />;
}
