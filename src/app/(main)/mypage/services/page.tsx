import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function MyServicesPage() {
  return <PagePlaceholder eyebrow="Agent My Page" title="Agent services route is ready." description="We will connect this route to getAgentServices, createService and updateService flows." />;
}
