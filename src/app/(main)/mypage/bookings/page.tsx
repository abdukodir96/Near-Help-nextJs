import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function MyBookingsPage() {
  return <PagePlaceholder eyebrow="My Page" title="Bookings route is ready." description="We will connect this route to getMyBookings and getAgentBookings with role-aware tabs." />;
}
