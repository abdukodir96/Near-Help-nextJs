import { PagePlaceholder } from '@/components/shared/page-placeholder';

export default function MyBookingsPage() {
  return <PagePlaceholder eyebrow="My Page / Bookings" title="Bookings page" description="This screen will connect to getMyBookings and update booking actions from the frontend." />;
}
