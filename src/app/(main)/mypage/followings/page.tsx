import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function MyFollowingsPage() {
  return <PagePlaceholder eyebrow="My Page" title="Followings route is ready." description="We will connect this route to getMemberFollowings and following management UI." />;
}
