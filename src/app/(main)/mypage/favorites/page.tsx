import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function MyFavoritesPage() {
  return <PagePlaceholder eyebrow="My Page" title="Favorites route is ready." description="We will map this route to getFavorites and render liked services." />;
}
