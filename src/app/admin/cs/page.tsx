import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function AdminCsPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin CS route is ready." description="We will connect this route to createNotice, getAllNoticesByAdmin, updateNoticeByAdmin and removeNoticeByAdmin." />;
}
