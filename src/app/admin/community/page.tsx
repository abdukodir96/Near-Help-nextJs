import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function AdminCommunityPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin community route is ready." description="We will connect this route to getAllArticlesByAdmin, updateArticleByAdmin and removeArticleByAdmin." />;
}
