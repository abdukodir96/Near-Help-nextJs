import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function AdminUsersPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin users route is ready." description="We will connect this route to getAllMembersByAdmin and updateMemberByAdmin." />;
}
