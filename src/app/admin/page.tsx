import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function AdminOverviewPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin dashboard route is ready." description="We will wire moderation summaries, counts and quick actions here." />;
}
