import {PagePlaceholder} from '@/components/shared/page-placeholder';

export default function AdminServicesPage() {
  return <PagePlaceholder eyebrow="Admin" title="Admin services route is ready." description="We will connect this route to getAllServicesByAdmin, updateServiceByAdmin and removeServiceByAdmin." />;
}
